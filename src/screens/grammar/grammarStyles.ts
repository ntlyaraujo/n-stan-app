/**
 * The class strings the Grammar Note screens use (#29–#34).
 *
 * The shared ones live in `src/components/styles.ts` and are re-exported here,
 * so importing from this file keeps working and there is still only one
 * definition of a button, an input or the focus ring.
 */

export {
  BADGE,
  BADGE_NEUTRAL,
  BUTTON,
  BUTTON_DANGER,
  BUTTON_PRIMARY,
  CARD,
  FOCUS_RING,
  INPUT,
  LABEL,
} from '../../components/styles.ts'
